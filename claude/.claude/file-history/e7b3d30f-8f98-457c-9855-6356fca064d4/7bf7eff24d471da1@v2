# ═══════════════════════════════════════════════════════════════
# 📦 Tmux Configuration - Catppuccin Mocha Theme
# ═══════════════════════════════════════════════════════════════

# ═══════════════════════════════════════════════════════════════
# 🔧 Basic Settings
# ═══════════════════════════════════════════════════════════════

set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"
set -g history-limit 100000

# Prefix: Ctrl+Space (mais acessível que Ctrl+b)
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# Enable mouse
set -g mouse on

# Start windows and panes at 1
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# Window & Pane Names
set -g automatic-rename on
set -g automatic-rename-format "#{b:pane_current_path}"
set -g set-titles on
set -g set-titles-string "#T"

# Activity & Bell
set -g monitor-activity on
set -g visual-activity on
set -g visual-bell off
set -g bell-action none

# ═══════════════════════════════════════════════════════════════
# ⌨️ Keybindings
# ═══════════════════════════════════════════════════════════════

# Reload config
bind r source-file ~/.config/tmux/tmux.conf \; display "Config reloaded!"

# Panes
bind - split-window -v -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"
bind _ split-window -fh -c "#{pane_current_path}"

# Navigate panes (vim-like)
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Resize panes
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# Rotate panes
bind o select-pane -t :.+

# Windows
bind c new-window -c "#{pane_current_path}"
bind C-c new-window -c "#{pane_current_path}"
bind p previous-window
bind n next-window
bind , command-prompt -I "#W" "rename-window '%%'"
bind w choose-window

# Sessions
bind s choose-session
bind S new-session
bind d detach-client

# Copy Mode (vim-like)
setw -g mode-keys vi
bind v copy-mode
bind P paste-buffer
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "xclip -in -selection clipboard"
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "xclip -in -selection clipboard"

# Clear screen
bind C-l send-keys 'C-l'

# Toggle pane zoom
bind z resize-pane -Z

# ═══════════════════════════════════════════════════════════════
# 🎨 Catppuccin Theme Configuration
# ═══════════════════════════════════════════════════════════════

set -g @catppuccin_flavour 'mocha'
set -g @catppuccin_status_background "none"
set -g @catppuccin_window_status_style "basic"
set -g @catppuccin_window_number_position "left"
set -g @catppuccin_window_middle_separator " "
set -g @catppuccin_window_default_number " #I"
set -g @catppuccin_window_current_number " #I"
set -g @catppuccin_window_default_format "#{?window_start_flag, ,}#{E:#{?pane_synchronized,🔒,}} #W "
set -g @catppuccin_window_current_format "#{?window_start_flag, ,}#{E:#{?pane_synchronized,🔒,}} #W "
set -g @catppuccin_status_left_separator " "
set -g @catppuccin_status_right_separator " "
set -g @catppuccin_status_connect_separator "no"
set -g @catppuccin_status_middle_separator "  "

# Status line position (top)
set -g status-position top

# Status Line
set -g status on
set -g status-interval 2
set -g status-justify left
set -g status-left-length 100
set -g status-right-length 100
set -g status-left "#[fg=mauve]❮ #S "

# Data e hora em pt-BR (dd-mm-aaaa)
set -g status-right "#{E:@catppuccin_status_application} #[fg=#cba6f7]%d-%m-%Y %H:%M "

# Formato de data pt-BR para logs
set -g @catppuccin_status_date_format "%d-%m-%Y"

run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux

# ═══════════════════════════════════════════════════════════════
# 🖼️ Pane & Window Borders - Cantos Arredondados & Translucidez
# ═══════════════════════════════════════════════════════════════

# Bordas com cantos arredondados (rounded corners)
set -g pane-active-border-style 'fg=#cba6f7,bg=default'
set -g pane-border-style 'fg=#45475a,bg=default'
set -g pane-border-status bottom

# Pane border format minimal (Starship-style)
set -g pane-border-format " #[fg=#cba6f7]#{pane_current_command} "

# Efeito translúcido estilo macOS para janelas
set -g window-active-style 'fg=#cdd6f4,bg=#1e1e2e'
set -g window-style 'fg=#a6adc8,bg=#1a1a26'

# Status line translúcida
set -g status-style 'bg=#1e1e2e,fg=#cdd6f4'

# ═══════════════════════════════════════════════════════════════
# ⚡ Advanced Features
# ═══════════════════════════════════════════════════════════════

# Escape time (faster response)
set -sg escape-time 10

# Focus events
set -g focus-events on

# Aggressive resize
setw -g aggressive-resize on

# Base index
set -g display-time 4000
set -g display-panes-time 2000

# ═══════════════════════════════════════════════════════════════
# 🎯 Plugin Manager (TPM)
# ═══════════════════════════════════════════════════════════════

# Install plugins: prefix + I
# Update plugins: prefix + U

# List of plugins
set -g @plugin 'catppuccin/tmux'

# Initialize TPM
run '~/.tmux/plugins/tpm/tpm'

# ═══════════════════════════════════════════════════════════════
# 🌙 Useful Commands
# ═══════════════════════════════════════════════════════════════

# Synchronize panes: prefix + *
bind * set -w synchronize-panes

# Break pane to new window: prefix + !
bind ! break-pane -d -n temp

# Join pane from another window: prefix + @
bind @ choose-window 'join-pane -s "%%"'

# Swap panes: prefix + }
bind '}' swap-pane -D
bind '{' swap-pane -U

# Move window: prefix + >
bind > move-window -t +1
bind < move-window -t -1
